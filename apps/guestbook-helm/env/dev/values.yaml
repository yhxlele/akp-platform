# Per-environment values for dev, layered over ../../chart/values.yaml.
# Kargo's yaml-update step bumps image.tag here on every promotion —
# this file IS the record of what runs in this environment.
replicaCount: 1

image:
  tag: v0.0.2
