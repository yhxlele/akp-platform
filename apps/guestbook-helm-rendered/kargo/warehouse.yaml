apiVersion: kargo.akuity.io/v1alpha1
kind: Warehouse
metadata:
  name: guestbook
  namespace: guestbook-helm-rendered
spec:
  subscriptions:
  # New semver tags of the (public) guestbook image produce new Freight.
  - image:
      repoURL: ghcr.io/akuity/guestbook
      imageSelectionStrategy: SemVer
      allowTagsRegexes:
      - ^v?[0-9]+\.[0-9]+\.[0-9]+([\+-].*)?$
  # Changes to this app's chart or values on main also produce new Freight.
  # Scoped with includePaths so promotions (which push to rendered/* branches)
  # and other apps' changes never trigger this warehouse.
  - git:
      repoURL: https://github.com/example-org/akp-platform.git
      branch: main
      includePaths:
      - apps/guestbook-helm-rendered
